# ─────────────────────────────────────────────────────────────
# Godot
# ─────────────────────────────────────────────────────────────
# 导入缓存与编辑器生成的元数据，全部可由引擎重新生成
.godot/
# 导出的可执行产物
/Output/
export_credentials.cfg

# 注意：以下两类**故意不忽略**，它们必须进版本库
#   · *.uid     —— Godot 4.4+ 会给每个脚本/着色器生成稳定 ID，
#                  场景文件靠它引用资源；丢了会导致引用失效
#   · *.import  —— 资源导入参数，缺失会导致重新导入后设置不一致

# ─────────────────────────────────────────────────────────────
# .NET / C#
# ─────────────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
*.userprefs

# ─────────────────────────────────────────────────────────────
# IDE
# ─────────────────────────────────────────────────────────────
.vs/
.idea/
*.swp
*.tmp

# ─────────────────────────────────────────────────────────────
# 本地资料（**不入库**）
# ─────────────────────────────────────────────────────────────
# docs/ 是设计/调研/调参记录，只作为本地资料保留：
#   · 改代码需要的信息都已在 README.md 与各文件顶部的类注释里；
#   · 注意代码注释里仍有 "docs/xxx.md §n" 这类指向（对本地开发有效，克隆下来没有这些文件）。
# 要重新纳入版本库：git add -f docs/
/docs/

# ─────────────────────────────────────────────────────────────
# 本项目的临时产物
# ─────────────────────────────────────────────────────────────
# 调试截图（用 Viewport.GetTexture().GetImage().SavePng 打出来的，只出现在项目根目录）。
# 注意连 .import 一起忽略 —— 截图放进项目根目录后 Godot 会给它生成导入文件，
# 只删 PNG 会留下孤儿 .import。
#
# ⚠ 这里刻意**不用** `/s*.png` 那种一个字母开头的写法：它会顺手把项目里任何
#   "s 开头的 png" 都忽略掉（真加资源时会被静默吞掉，很难查）。按前缀点名即可。
/shot*.png
/shot*.png.import
/*.log

# ─────────────────────────────────────────────────────────────
# Godot
# ─────────────────────────────────────────────────────────────
# 导入缓存与编辑器生成的元数据，全部可由引擎重新生成
.godot/
# 导出的可执行产物
/Output/
export_credentials.cfg

# 注意：以下两类**故意不忽略**，它们必须进版本库
#   · *.uid     —— Godot 4.4+ 会给每个脚本/着色器生成稳定 ID，
#                  场景文件靠它引用资源；丢了会导致引用失效
#   · *.import  —— 资源导入参数，缺失会导致重新导入后设置不一致

# ─────────────────────────────────────────────────────────────
# .NET / C#
# ─────────────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
*.userprefs

# ─────────────────────────────────────────────────────────────
# IDE
# ─────────────────────────────────────────────────────────────
.vs/
.idea/
*.swp
*.tmp

# ─────────────────────────────────────────────────────────────
# 本项目的临时产物
# ─────────────────────────────────────────────────────────────
# 调试截图（用 Viewport.GetTexture().GetImage().SavePng 打出来的）。
# 注意连 .import 一起忽略 —— 截图放进项目根目录后 Godot 会给它生成导入文件，
# 只删 PNG 会留下孤儿 .import。
/s*.png
/s*.png.import
/shot*.png
/shot*.png.import
/*.log
